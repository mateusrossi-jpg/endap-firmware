--- a/components/runtime/failsafe/include/failsafe.h
+++ b/components/runtime/failsafe/include/failsafe.h
@@ -81,6 +81,7 @@
 void failsafe_init(void);
 bool failsafe_load(void);
 bool failsafe_save(void);
+int failsafe_sync_outputs(void);
 
 bool failsafe_action_from_code(const char *text, failsafe_action_t *out_action);
 const char *failsafe_action_to_code(failsafe_action_t action);
--- a/components/runtime/failsafe/failsafe.c
+++ b/components/runtime/failsafe/failsafe.c
@@ -1,6 +1,7 @@
 #include "failsafe.h"
 
 #include "device_profile.h"
+#include "io_binding.h"
 #include "state.h"
 
 #include "esp_log.h"
@@ -183,6 +184,34 @@
     entry->last_applied_value = 0;
 }
 
+static bool failsafe_output_is_active(uint16_t output_id)
+{
+    return output_id != 0U &&
+           device_profile_is_valid_output(output_id) &&
+           io_binding_has_output(output_id);
+}
+
+static failsafe_runtime_entry_t *failsafe_ensure_entry(uint16_t output_id, bool require_active)
+{
+    failsafe_runtime_entry_t *entry = failsafe_find(output_id);
+
+    if (entry)
+        return entry;
+
+    if (output_id == 0U || !device_profile_is_valid_output(output_id))
+        return NULL;
+
+    if (require_active && !io_binding_has_output(output_id))
+        return NULL;
+
+    if (entry_count >= FAILSAFE_MAX_OUTPUTS)
+        return NULL;
+
+    failsafe_default_entry(&entries[entry_count], output_id);
+    entry_count++;
+    return &entries[entry_count - 1U];
+}
+
 static failsafe_reason_t failsafe_detect_boot_reason(void)
 {
     switch (esp_reset_reason())
@@ -259,7 +288,7 @@
         return false;
     }
 
-    entry = failsafe_find(policy->output_id);
+    entry = failsafe_ensure_entry(policy->output_id, true);
     if (!entry)
         return false;
 
@@ -407,26 +436,49 @@
 void failsafe_init(void)
 {
     bool loaded;
+    io_binding_output_view_t outputs[IO_BINDING_MAX_OUTPUTS];
+    int count;
 
     entry_count = 0U;
     memset(entries, 0, sizeof(entries));
     boot_reason = failsafe_detect_boot_reason();
 
-    for (int i = 0; i < device_profile_output_count() && entry_count < FAILSAFE_MAX_OUTPUTS; i++)
+    count = io_binding_export_outputs(outputs, IO_BINDING_MAX_OUTPUTS);
+    for (int i = 0; i < count; i++)
     {
-        uint16_t output_id = device_profile_output_id_at(i);
-        if (output_id == 0U)
+        if (outputs[i].id == 0U)
             continue;
-
-        failsafe_default_entry(&entries[entry_count], output_id);
-        entry_count++;
+        (void)failsafe_ensure_entry(outputs[i].id, true);
     }
 
     loaded = failsafe_load();
+    (void)failsafe_sync_outputs();
     if (!loaded)
         (void)failsafe_save();
 }
 
+int failsafe_sync_outputs(void)
+{
+    io_binding_output_view_t outputs[IO_BINDING_MAX_OUTPUTS];
+    int count = io_binding_export_outputs(outputs, IO_BINDING_MAX_OUTPUTS);
+    int created = 0;
+
+    for (int i = 0; i < count; i++)
+    {
+        uint16_t output_id = outputs[i].id;
+        if (output_id == 0U)
+            continue;
+
+        if (!failsafe_find(output_id) && failsafe_ensure_entry(output_id, true))
+            created++;
+    }
+
+    if (created > 0)
+        (void)failsafe_save();
+
+    return created;
+}
+
 bool failsafe_action_from_code(const char *text, failsafe_action_t *out_action)
 {
     if (!text || !out_action)
@@ -602,7 +654,7 @@
                                 int32_t safe_value,
                                 failsafe_recovery_t recovery_mode)
 {
-    failsafe_runtime_entry_t *entry = failsafe_find(output_id);
+    failsafe_runtime_entry_t *entry = failsafe_ensure_entry(output_id, true);
     failsafe_policy_storage_t next;
 
     if (!entry ||
@@ -637,7 +689,7 @@
 
 bool failsafe_get_output_policy(uint16_t output_id, failsafe_output_status_t *out)
 {
-    const failsafe_runtime_entry_t *entry = failsafe_find_const(output_id);
+    const failsafe_runtime_entry_t *entry = failsafe_ensure_entry(output_id, true);
 
     if (!entry || !out)
         return false;
@@ -674,15 +726,25 @@
 
 int failsafe_export(failsafe_output_status_t *out, int max_outputs)
 {
-    int copy_count = ((int)entry_count < max_outputs) ? (int)entry_count : max_outputs;
+    int exported = 0;
+    int total_active = 0;
 
-    if (!out || max_outputs <= 0)
-        return (int)entry_count;
+    (void)failsafe_sync_outputs();
 
-    for (int i = 0; i < copy_count; i++)
-        failsafe_fill_status(&entries[i], &out[i]);
+    for (uint16_t i = 0U; i < entry_count; i++)
+    {
+        if (!failsafe_output_is_active(entries[i].policy.output_id))
+            continue;
 
-    return copy_count;
+        total_active++;
+        if (out && max_outputs > 0 && exported < max_outputs)
+        {
+            failsafe_fill_status(&entries[i], &out[exported]);
+            exported++;
+        }
+    }
+
+    return (out && max_outputs > 0) ? exported : total_active;
 }
 
 bool failsafe_startup_value(uint16_t output_id,
@@ -690,7 +752,7 @@
                             int32_t *out_value,
                             const char **out_origin)
 {
-    failsafe_runtime_entry_t *entry = failsafe_find(output_id);
+    failsafe_runtime_entry_t *entry = failsafe_ensure_entry(output_id, true);
     failsafe_action_t action;
     int32_t value;
 
@@ -728,7 +790,7 @@
                             int32_t *out_effective_value,
                             const char **out_reason)
 {
-    failsafe_runtime_entry_t *entry = failsafe_find(output_id);
+    failsafe_runtime_entry_t *entry = failsafe_ensure_entry(output_id, true);
 
     (void)source;
 
@@ -759,7 +821,7 @@
                                failsafe_reason_t reason,
                                int32_t *out_safe_value)
 {
-    failsafe_runtime_entry_t *entry = failsafe_find(output_id);
+    failsafe_runtime_entry_t *entry = failsafe_ensure_entry(output_id, true);
     failsafe_action_t action;
     int32_t current_value = 0;
     int32_t applied_value;
@@ -789,11 +851,16 @@
 {
     int applied = 0;
 
+    (void)failsafe_sync_outputs();
+
     for (uint16_t i = 0U; i < entry_count; i++)
     {
         int32_t safe_value = 0;
         uint16_t output_id = entries[i].policy.output_id;
 
+        if (!failsafe_output_is_active(output_id))
+            continue;
+
         if (!failsafe_apply_for_reason(output_id, reason, &safe_value))
             continue;
 
@@ -814,7 +881,7 @@
 
 bool failsafe_clear_manual_reset(uint16_t output_id)
 {
-    failsafe_runtime_entry_t *entry = failsafe_find(output_id);
+    failsafe_runtime_entry_t *entry = failsafe_ensure_entry(output_id, true);
 
     if (!entry)
         return false;
@@ -828,7 +895,7 @@
 {
     const failsafe_runtime_entry_t *entry = failsafe_find_const(output_id);
 
-    return entry && entry->active != 0U;
+    return entry && failsafe_output_is_active(output_id) && entry->active != 0U;
 }
 
 bool failsafe_trigger_comm_loss(uint16_t output_id, int32_t *out_safe_value)
--- a/components/runtime/failsafe/CMakeLists.txt
+++ b/components/runtime/failsafe/CMakeLists.txt
@@ -3,6 +3,7 @@
     INCLUDE_DIRS "include"
     REQUIRES
         device_profile
+        io_binding
         nvs_flash
         state
 )
--- a/components/runtime/http_server/http_server.c
+++ b/components/runtime/http_server/http_server.c
@@ -2892,6 +2892,7 @@
         return 0U;
 
     buf[0] = '\0';
+    (void)failsafe_sync_outputs();
     count = failsafe_export(failsafe_status_snapshot, FAILSAFE_MAX_OUTPUTS);
 
     if (!append_format(buf,
@@ -2904,6 +2905,12 @@
     for (int i = 0; i < count; i++)
     {
         const failsafe_output_status_t *status = &failsafe_status_snapshot[i];
+        io_binding_output_view_t binding = {0};
+        bool has_binding = io_binding_get_output(status->output_id, &binding);
+        int32_t current_value = 0;
+        const char *backend_code = has_binding ? io_binding_backend_code(binding.backend) : "";
+
+        (void)state_get_int(status->output_id, &current_value);
 
         if (i > 0 && !append_text(buf, buf_size, &offset, ","))
             return 0U;
@@ -2911,8 +2918,50 @@
         if (!append_format(buf,
                            buf_size,
                            &offset,
-                           "{\"output_id\":%u,\"enabled\":%u,\"boot_action\":",
+                           "{\"id\":%u,\"output_id\":%u,\"name\":",
                            status->output_id,
+                           status->output_id))
+            return 0U;
+
+        if (!append_json_string(buf, buf_size, &offset, has_binding ? binding.name : ""))
+            return 0U;
+
+        if (!append_text(buf, buf_size, &offset, ",\"alias\":"))
+            return 0U;
+
+        if (!append_json_string(buf, buf_size, &offset, has_binding ? binding.name : ""))
+            return 0U;
+
+        if (!append_text(buf, buf_size, &offset, ",\"role\":"))
+            return 0U;
+
+        if (!append_json_string(buf, buf_size, &offset, has_binding ? binding.role : ""))
+            return 0U;
+
+        if (!append_text(buf, buf_size, &offset, ",\"backend\":"))
+            return 0U;
+
+        if (!append_json_string(buf, buf_size, &offset, backend_code ? backend_code : ""))
+            return 0U;
+
+        if (!append_text(buf, buf_size, &offset, ",\"backend_name\":"))
+            return 0U;
+
+        if (!append_json_string(buf, buf_size, &offset, has_binding ? binding.backend_name : ""))
+            return 0U;
+
+        if (!append_text(buf, buf_size, &offset, ",\"address\":"))
+            return 0U;
+
+        if (!append_json_string(buf, buf_size, &offset, has_binding ? binding.address : ""))
+            return 0U;
+
+        if (!append_format(buf,
+                           buf_size,
+                           &offset,
+                           ",\"gpio\":%d,\"value\":%" PRId32 ",\"enabled\":%u,\"boot_action\":",
+                           has_binding ? binding.gpio : -1,
+                           current_value,
                            status->enabled ? 1U : 0U))
             return 0U;
 
@@ -2954,10 +3003,7 @@
         if (!append_json_string(buf, buf_size, &offset, failsafe_reason_name(status->last_reason)))
             return 0U;
 
-        if (!append_format(buf,
-                           buf_size,
-                           &offset,
-                           ",\"startup_mode\":"))
+        if (!append_text(buf, buf_size, &offset, ",\"startup_mode\":"))
             return 0U;
 
         if (!append_json_string(buf, buf_size, &offset, failsafe_mode_to_code(status->startup_mode)))
@@ -5690,6 +5736,61 @@
     return ESP_OK;
 }
 
+static bool query_get_channel_backend(httpd_req_t *req, device_channel_backend_t *out_backend)
+{
+    char backend_text[24] = {0};
+    bool found = false;
+
+    if (!out_backend)
+        return false;
+
+    *out_backend = DEVICE_CHANNEL_BACKEND_GPIO;
+
+    if (!query_get_optional_str(req, "backend", backend_text, sizeof(backend_text), &found))
+        return false;
+
+    if (!found || backend_text[0] == '\0' || strcmp(backend_text, "gpio") == 0)
+    {
+        *out_backend = DEVICE_CHANNEL_BACKEND_GPIO;
+        return true;
+    }
+
+    if (strcmp(backend_text, "mcp23x17") == 0 || strcmp(backend_text, "mcp") == 0)
+    {
+        *out_backend = DEVICE_CHANNEL_BACKEND_MCP23X17;
+        return true;
+    }
+
+    return false;
+}
+
+static bool query_get_binding_address(httpd_req_t *req,
+                                      device_channel_backend_t backend,
+                                      int *gpio,
+                                      int *backend_instance,
+                                      int *endpoint_index)
+{
+    if (!gpio || !backend_instance || !endpoint_index)
+        return false;
+
+    *gpio = -1;
+    *backend_instance = 0;
+    *endpoint_index = 0;
+
+    if (backend == DEVICE_CHANNEL_BACKEND_GPIO)
+    {
+        return query_get_optional_int(req, "gpio", -1, INT16_MAX, gpio, NULL);
+    }
+
+    if (backend == DEVICE_CHANNEL_BACKEND_MCP23X17)
+    {
+        return query_get_optional_int(req, "backend_instance", 0, INT16_MAX, backend_instance, NULL) &&
+               query_get_optional_int(req, "endpoint_index", 0, INT16_MAX, endpoint_index, NULL);
+    }
+
+    return false;
+}
+
 static esp_err_t respond_binding_result(httpd_req_t *req, io_binding_result_t result)
 {
     switch (result)
@@ -5722,6 +5823,26 @@
             httpd_resp_sendstr(req, "PROTECTED_SLOT");
             return ESP_FAIL;
 
+        case IO_BINDING_RESULT_INVALID_BACKEND:
+            httpd_resp_set_status(req, "409 Conflict");
+            httpd_resp_sendstr(req, "INVALID_BACKEND");
+            return ESP_FAIL;
+
+        case IO_BINDING_RESULT_UNSUPPORTED_BACKEND:
+            httpd_resp_set_status(req, "409 Conflict");
+            httpd_resp_sendstr(req, "UNSUPPORTED_BACKEND");
+            return ESP_FAIL;
+
+        case IO_BINDING_RESULT_INVALID_ENDPOINT:
+            httpd_resp_set_status(req, "409 Conflict");
+            httpd_resp_sendstr(req, "INVALID_ENDPOINT");
+            return ESP_FAIL;
+
+        case IO_BINDING_RESULT_ADDRESS_CONFLICT:
+            httpd_resp_set_status(req, "409 Conflict");
+            httpd_resp_sendstr(req, "ADDRESS_CONFLICT");
+            return ESP_FAIL;
+
         case IO_BINDING_RESULT_NOT_FOUND:
         default:
             httpd_resp_set_status(req, "404 Not Found");
@@ -5734,6 +5855,9 @@
 {
     int id = 0;
     int gpio = -1;
+    int backend_instance = 0;
+    int endpoint_index = 0;
+    device_channel_backend_t backend = DEVICE_CHANNEL_BACKEND_GPIO;
     char name[IO_BINDING_NAME_LEN] = {0};
     char role[IO_BINDING_ROLE_LEN] = {0};
     io_binding_result_t result;
@@ -5747,19 +5871,20 @@
     if (!query_get_int(req, "id", 0, UINT16_MAX, &id) ||
         !query_get_optional_str(req, "name", name, sizeof(name), NULL) ||
         !query_get_optional_str(req, "role", role, sizeof(role), NULL) ||
-        !query_get_optional_int(req, "gpio", -1, INT16_MAX, &gpio, NULL))
+        !query_get_channel_backend(req, &backend) ||
+        !query_get_binding_address(req, backend, &gpio, &backend_instance, &endpoint_index))
     {
         httpd_resp_set_status(req, "400 Bad Request");
         httpd_resp_sendstr(req, "BAD_REQUEST");
         return ESP_OK;
     }
 
-    result = io_binding_set_output((uint16_t)id, name, role, gpio);
+    result = io_binding_set_output_ex((uint16_t)id, name, role, backend, gpio, backend_instance, endpoint_index);
 
     if (respond_binding_result(req, result) != ESP_OK)
         return ESP_OK;
 
-    snprintf(audit_detail, sizeof(audit_detail), "output=%d gpio=%d name=%s", id, gpio, name[0] ? name : "-");
+    snprintf(audit_detail, sizeof(audit_detail), "output=%d backend=%s address=%d:%d gpio=%d name=%s", id, io_binding_backend_code(backend), backend_instance, endpoint_index, gpio, name[0] ? name : "-");
     auth_audit_log("output_config_updated", audit_detail);
     http_server_notify_state_change();
     httpd_resp_sendstr(req, "SAVED");
@@ -5770,6 +5895,9 @@
 {
     int id = 0;
     int gpio = -1;
+    int backend_instance = 0;
+    int endpoint_index = 0;
+    device_channel_backend_t backend = DEVICE_CHANNEL_BACKEND_GPIO;
     char name[IO_BINDING_NAME_LEN] = {0};
     char role[IO_BINDING_ROLE_LEN] = {0};
     io_binding_result_t result;
@@ -5783,19 +5911,20 @@
     if (!query_get_int(req, "id", 0, UINT16_MAX, &id) ||
         !query_get_optional_str(req, "name", name, sizeof(name), NULL) ||
         !query_get_optional_str(req, "role", role, sizeof(role), NULL) ||
-        !query_get_optional_int(req, "gpio", -1, INT16_MAX, &gpio, NULL))
+        !query_get_channel_backend(req, &backend) ||
+        !query_get_binding_address(req, backend, &gpio, &backend_instance, &endpoint_index))
     {
         httpd_resp_set_status(req, "400 Bad Request");
         httpd_resp_sendstr(req, "BAD_REQUEST");
         return ESP_OK;
     }
 
-    result = io_binding_set_input((uint16_t)id, name, role, gpio);
+    result = io_binding_set_input_ex((uint16_t)id, name, role, backend, gpio, backend_instance, endpoint_index);
 
     if (respond_binding_result(req, result) != ESP_OK)
         return ESP_OK;
 
-    snprintf(audit_detail, sizeof(audit_detail), "input=%d gpio=%d name=%s", id, gpio, name[0] ? name : "-");
+    snprintf(audit_detail, sizeof(audit_detail), "input=%d backend=%s address=%d:%d gpio=%d name=%s", id, io_binding_backend_code(backend), backend_instance, endpoint_index, gpio, name[0] ? name : "-");
     auth_audit_log("input_config_updated", audit_detail);
     http_server_notify_state_change();
     httpd_resp_sendstr(req, "SAVED");
@@ -5862,6 +5991,9 @@
 {
     int id = 0;
     int gpio = -1;
+    int backend_instance = 0;
+    int endpoint_index = 0;
+    device_channel_backend_t backend = DEVICE_CHANNEL_BACKEND_GPIO;
     char name[IO_BINDING_NAME_LEN] = {0};
     char role[IO_BINDING_ROLE_LEN] = {0};
     io_binding_result_t result;
@@ -5873,21 +6005,22 @@
     http_set_private_text_headers(req);
 
     if (!query_get_int(req, "id", 0, UINT16_MAX, &id) ||
-        !query_get_int(req, "gpio", 0, INT16_MAX, &gpio) ||
         !query_get_optional_str(req, "name", name, sizeof(name), NULL) ||
-        !query_get_optional_str(req, "role", role, sizeof(role), NULL))
+        !query_get_optional_str(req, "role", role, sizeof(role), NULL) ||
+        !query_get_channel_backend(req, &backend) ||
+        !query_get_binding_address(req, backend, &gpio, &backend_instance, &endpoint_index))
     {
         httpd_resp_set_status(req, "400 Bad Request");
         httpd_resp_sendstr(req, "BAD_REQUEST");
         return ESP_OK;
     }
 
-    result = io_binding_add_input((uint16_t)id, name, role, gpio);
+    result = io_binding_add_input_ex((uint16_t)id, name, role, backend, gpio, backend_instance, endpoint_index);
 
     if (respond_binding_result(req, result) != ESP_OK)
         return ESP_OK;
 
-    snprintf(audit_detail, sizeof(audit_detail), "input=%d gpio=%d", id, gpio);
+    snprintf(audit_detail, sizeof(audit_detail), "input=%d backend=%s address=%d:%d gpio=%d", id, io_binding_backend_code(backend), backend_instance, endpoint_index, gpio);
     auth_audit_log("input_added", audit_detail);
     http_server_notify_state_change();
     httpd_resp_sendstr(req, "ADDED");
@@ -5898,6 +6031,9 @@
 {
     int id = 0;
     int gpio = -1;
+    int backend_instance = 0;
+    int endpoint_index = 0;
+    device_channel_backend_t backend = DEVICE_CHANNEL_BACKEND_GPIO;
     char name[IO_BINDING_NAME_LEN] = {0};
     char role[IO_BINDING_ROLE_LEN] = {0};
     io_binding_result_t result;
@@ -5909,21 +6045,23 @@
     http_set_private_text_headers(req);
 
     if (!query_get_int(req, "id", 0, UINT16_MAX, &id) ||
-        !query_get_int(req, "gpio", 0, INT16_MAX, &gpio) ||
         !query_get_optional_str(req, "name", name, sizeof(name), NULL) ||
-        !query_get_optional_str(req, "role", role, sizeof(role), NULL))
+        !query_get_optional_str(req, "role", role, sizeof(role), NULL) ||
+        !query_get_channel_backend(req, &backend) ||
+        !query_get_binding_address(req, backend, &gpio, &backend_instance, &endpoint_index))
     {
         httpd_resp_set_status(req, "400 Bad Request");
         httpd_resp_sendstr(req, "BAD_REQUEST");
         return ESP_OK;
     }
 
-    result = io_binding_add_output((uint16_t)id, name, role, gpio);
+    result = io_binding_add_output_ex((uint16_t)id, name, role, backend, gpio, backend_instance, endpoint_index);
 
     if (respond_binding_result(req, result) != ESP_OK)
         return ESP_OK;
 
-    snprintf(audit_detail, sizeof(audit_detail), "output=%d gpio=%d", id, gpio);
+    (void)failsafe_sync_outputs();
+    snprintf(audit_detail, sizeof(audit_detail), "output=%d backend=%s address=%d:%d gpio=%d", id, io_binding_backend_code(backend), backend_instance, endpoint_index, gpio);
     auth_audit_log("output_added", audit_detail);
     http_server_notify_state_change();
     httpd_resp_sendstr(req, "ADDED");
@@ -5980,6 +6118,7 @@
     if (respond_binding_result(req, result) != ESP_OK)
         return ESP_OK;
 
+    (void)failsafe_sync_outputs();
     auth_audit_log("output_removed", "binding-removed");
     http_server_notify_state_change();
     httpd_resp_sendstr(req, "REMOVED");
--- a/components/dashboard/index.html
+++ b/components/dashboard/index.html
@@ -68,6 +68,7 @@
       <button class="tab-btn" data-tab="connect" onclick="setTab('connect')">Conectar</button>
       <button class="tab-btn" data-tab="automation" onclick="setTab('automation')">Automação</button>
       <button class="tab-btn" data-tab="operations" onclick="setTab('operations')">Operação</button>
+      <button class="tab-btn" data-tab="failsafe" onclick="setTab('failsafe')">Fail-Safe</button>
       <button class="tab-btn" data-tab="nodes" onclick="setTab('nodes')">Nós</button>
     </div>
     <div class="nav">
@@ -312,6 +313,18 @@
       </div>
     </section>
 
+    <section id="tab_failsafe" class="tab-panel">
+      <div class="hero"><div class="kicker">Segurança das saídas</div><h1>Fail-safe por saída, visível e editável.</h1><p>Configure o comportamento seguro de cada saída ativa no boot, na perda de comunicação e em falha de runtime/watchdog. As saídas listadas vêm do binding/perfil ativo do nó.</p><div class="hero-actions"><button class="primary-btn" onclick="loadFailsafeOutputs(true)">Atualizar fail-safe</button><button class="soft-btn" onclick="setTab('nodes')">Adicionar saída</button></div></div>
+      <div class="micro-grid">
+        <div class="micro-card"><div class="label">Saídas ativas</div><div id="failsafe_total" class="value">-</div><div id="failsafe_total_sub" class="sub">Aguardando API.</div></div>
+        <div class="micro-card"><div class="label">Protegidas</div><div id="failsafe_enabled_total" class="value">-</div><div id="failsafe_enabled_sub" class="sub">Política habilitada.</div></div>
+        <div class="micro-card"><div class="label">Em fail-safe</div><div id="failsafe_active_total" class="value">-</div><div id="failsafe_active_sub" class="sub">Bloqueio/rearme atual.</div></div>
+        <div class="micro-card"><div class="label">Rearme manual</div><div id="failsafe_manual_total" class="value">-</div><div id="failsafe_manual_sub" class="sub">Exige intervenção.</div></div>
+      </div>
+      <div class="card"><div class="kicker">Políticas por canal</div><h2>Saídas configuradas</h2><div class="note-strip">Para criar mais saídas, abra <strong>Nós &gt; modo técnico &gt; Binding local do nó &gt; Adicionar saída</strong>. Depois de adicionada, a política fail-safe aparece aqui automaticamente com padrão seguro FORCE_OFF.</div><div id="failsafe_outputs" class="section-stack" style="margin-top:14px"><div class="empty">Carregando fail-safe.</div></div></div>
+      <div id="failsafe_advanced" class="advanced-shell"><div class="card"><div class="kicker">Teste técnico</div><h2>Validação operacional</h2><div class="list"><div class="item"><div><strong>Botão Testar</strong><span>Disponível somente no modo técnico e para papel com permissão de profile_write. Ele força a política de teste da saída selecionada.</span></div></div><div class="item"><div><strong>Regra de precedência</strong><span>Quando o fail-safe manual estiver ativo, comando manual/remoto/automação deve ser bloqueado até rearme.</span></div></div></div></div></div>
+    </section>
+
     <section id="tab_nodes" class="tab-panel">
       <div class="hero"><div class="kicker">Nós do sistema</div><h1>Veja quais ESPs estão online e abra os detalhes de cada um.</h1><p>A lista de nós mostra status, IP, transporte e saúde. Os detalhes técnicos completos ficam no modo técnico. Nesta rodada final, os painéis foram compactados para reduzir áreas vazias e facilitar a leitura.</p></div>
       <div class="micro-grid">
@@ -337,6 +350,7 @@
 const $=id=>document.getElementById(id);
 let currentTab='panel', advancedMode=false, ws=null, wsConnected=false;
 let lastStatus=null, profile={inputs:[],outputs:[],network:{}}, nodes=[], wifiStatus={}, wifiNetworks=[], selectedNodeId=0, automations=[];
+let failsafeOutputs={outputs:[],loaded:false,error:''};
 let selectedNodeProfileContext=null, showInactiveComponents=false;
 const remoteCache={}; const TIMED_MODES=['pulse_ms','on_delay_ms','off_delay_ms'];
 let authState={authenticated:false,configured:false,bootstrap_open:false,bootstrap_required:false,password_change_required:false,role:'',username:'',session_expires_in:0,session_timeout_seconds:0,retry_after:0,capabilities:{},backendReady:false};
@@ -426,7 +440,7 @@
 function html(id,v){const el=typeof id==='string'?$(id):id; if(el) el.innerHTML=v==null?'':String(v)}
 function n(v){return Number(v)||0}
 function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
-function setTab(tab){if(!authAllowsDashboard()) return; currentTab=tab; document.querySelectorAll('.tab-btn').forEach(b=>b.classList.toggle('active',b.dataset.tab===tab)); document.querySelectorAll('.tab-panel').forEach(p=>p.classList.toggle('active',p.id===`tab_${tab}`)); if(tab==='automation') loadAutomation(); if(tab==='nodes') loadNodes(); if(tab==='operations') loadOperations(false);}
+function setTab(tab){if(!authAllowsDashboard()) return; currentTab=tab; document.querySelectorAll('.tab-btn').forEach(b=>b.classList.toggle('active',b.dataset.tab===tab)); document.querySelectorAll('.tab-panel').forEach(p=>p.classList.toggle('active',p.id===`tab_${tab}`)); if(tab==='automation') loadAutomation(); if(tab==='nodes') loadNodes(); if(tab==='operations') loadOperations(false); if(tab==='failsafe') loadFailsafeOutputs(true);}
 function toggleAdvancedMode(){if(!authAllowsDashboard()) return; advancedMode=!advancedMode; text('advanced_toggle',advancedMode?'Ocultar modo técnico':'Modo técnico'); document.querySelectorAll('.advanced-shell').forEach(el=>el.classList.toggle('active',advancedMode)); renderAll();}
 function apiJson(url,opts){return apiFetch(url,opts).then(r=>r.ok?r.json():Promise.reject(r.status))}
 function apiText(url,opts){return apiFetch(url,opts).then(async r=>({ok:r.ok,text:await r.text()}))}
@@ -592,7 +606,7 @@
  },forceNow?0:Math.max(80,delayMs||0));
 }
 function renderNodeSummaryCards(){const total=nodes.length; const online=nodes.filter(node=>(node.is_local)|| (nodeIsOperational(node)&&String(node.cluster_state||"").toLowerCase().includes("online"))).length; const attention=nodes.filter(node=>{const s=String(node.cluster_state||"").toLowerCase(); return nodeIsOperational(node) && nodeHasVisibleTransport(node) && (s.includes("suspect")||s.includes("offline")||s.includes("degraded"));}).length; const selected=nodes.find(node=>n(node.id)===n(selectedNodeId)); text("node_total",total); text("node_total_sub",total?"Nós visíveis no momento.":"Nenhum nó visível agora."); text("node_online",online); text("node_online_sub",online?"Nós operacionais respondendo agora.":"Sem nós operacionais online."); text("node_attention",attention); text("node_attention_sub",attention?"Existe nó operacional exigindo atenção.":"Nenhum alerta importante nos nós ativos."); text("node_selected",selected?`ESP ${nodes.findIndex(node=>node.id===selected.id)+1}`:'-'); text("node_selected_sub",selected?`${nodeDisplayStateLabel(selected)} · ${selected.ip||'Sem IP'} · ${nodeTransportBadgeText(selected)}`:'Escolha um nó para ver o detalhe.');}
-function renderAll(){renderPanel(); renderConnect(); renderLearn(); renderScalabilityOverview(); renderChannelInventory(); renderNodes(); renderPinConfig(); renderSlotManagement(); renderMcpRuntime(); renderAutomationOverview(); renderOperations(); updateAutoPreview(); updateTechPreview();}
+function renderAll(){renderPanel(); renderConnect(); renderLearn(); renderScalabilityOverview(); renderChannelInventory(); renderNodes(); renderPinConfig(); renderSlotManagement(); renderMcpRuntime(); renderAutomationOverview(); renderOperations(); renderFailsafeTab(); updateAutoPreview(); updateTechPreview();}
 function activeRoute(d){const flags=protocolFlags(networkConfig()); if(!d){return flags.wifi||flags.ethernet||flags.rs485?protocolSummaryLabel(flags):'Sem conexão';} if(d.net_ready && d.net_active_name){const name=String(d.net_active_name).toUpperCase(); const ip=d.net_active_ip_text||''; return ip?`${name} ${ip}`:name;} if(flags.wifi && d.wifi_state && d.wifi_state!=='disabled') return `WI-FI ${d.wifi_state}`; if(flags.ethernet) return 'ETHERNET aguardando link'; if(flags.rs485) return `RS485 ${rs485Label(d)}`; return 'Sem conexão';}
 const STARTUP_GRACE_MS=15000;
 function uptimeMs(d){return Number(d&&d.uptime_ms)||0;}
@@ -750,9 +764,14 @@
  const actions=canWrite?`<div class="actions"><button type="button" class="soft-btn" onclick="return saveFailsafePolicy(this,${n(it.id)},'${key}')">Salvar fail-safe</button>${active?`<button type="button" class="primary-btn" onclick="return rearmFailsafeOutput(this,${n(it.id)})">Rearmar</button>`:''}${advancedMode?`<button type="button" class="danger-btn" onclick="return testFailsafeOutput(this,${n(it.id)})">Testar</button>`:''}</div>`:'<div class="muted-line">Seu papel atual pode visualizar, mas não alterar fail-safe.</div>';
  return `<details style="margin-top:10px"><summary>Fail-safe da saída · ${esc(title)}</summary><div class="form-grid" style="margin-top:12px"><div class="field"><label>Ativo</label><select id="fs_enabled_${key}" ${disabled}><option value="1" ${enabled?'selected':''}>Sim</option><option value="0" ${!enabled?'selected':''}>Não</option></select></div><div class="field"><label>Boot/reboot</label><select id="fs_boot_${key}" ${disabled}>${failsafeActionOptionsHtml(boot,'boot')}</select></div><div class="field"><label>Perda de comunicação</label><select id="fs_comm_${key}" ${disabled}>${failsafeActionOptionsHtml(comm,'comm')}</select></div><div class="field"><label>Falha runtime/watchdog</label><select id="fs_runtime_${key}" ${disabled}>${failsafeActionOptionsHtml(runtime,'runtime')}</select></div><div class="field"><label>Valor seguro</label><select id="fs_safe_${key}" ${disabled}><option value="0" ${safe?'':'selected'}>OFF</option><option value="1" ${safe?'selected':''}>ON</option></select></div><div class="field"><label>Recuperação</label><select id="fs_recovery_${key}" ${disabled}>${failsafeRecoveryOptionsHtml(recovery)}</select></div></div><div class="note-strip" style="margin-top:10px">${esc(channelFailsafeText(st)||'Política segura padrão: FORCE_OFF em boot, perda de comunicação e falha.')}</div>${actions}</details>`;
 }
-function saveFailsafePolicy(btn,outputId,key){if(!authCanUse('profile_write')){afterError('Acesso negado','Seu papel atual nao pode alterar fail-safe.'); return false;} const fields={output_id:outputId,enabled:$(`fs_enabled_${key}`)?$(`fs_enabled_${key}`).value:'1',boot_action:$(`fs_boot_${key}`)?$(`fs_boot_${key}`).value:'force_off',comm_loss_action:$(`fs_comm_${key}`)?$(`fs_comm_${key}`).value:'force_off',runtime_fault_action:$(`fs_runtime_${key}`)?$(`fs_runtime_${key}`).value:'force_off',safe_value:$(`fs_safe_${key}`)?$(`fs_safe_${key}`).value:'0',recovery_mode:$(`fs_recovery_${key}`)?$(`fs_recovery_${key}`).value:'auto'}; markNodeDetailInteraction(30000); setInlineStatus('tone-info','Salvando fail-safe','A política segura da saída está sendo persistida.',true); withBusy(btn,'Salvando',()=>apiForm('/api/failsafe/output/save',fields).then(({ok,data})=>{if(!ok||data&&data.ok===false){afterError('Falha ao salvar fail-safe',(data&&data.error)||'O backend recusou a política.'); return;} return Promise.all([loadStatus(true),currentTab==='operations'?loadOperations(true):Promise.resolve()]).then(()=>{if(currentTab==='nodes') renderSelectedNodeDetail(false); afterSuccess('Fail-safe salvo','A política da saída foi atualizada.');});}).catch(()=>afterError('Falha ao salvar fail-safe','Não foi possível salvar a política agora.'))); return false;}
-function rearmFailsafeOutput(btn,outputId){if(!authCanUse('profile_write')){afterError('Acesso negado','Seu papel atual nao pode rearmar fail-safe.'); return false;} markNodeDetailInteraction(30000); withBusy(btn,'Rearmando',()=>apiForm('/api/failsafe/output/reset',{output_id:outputId}).then(({ok,data})=>{if(!ok||data&&data.ok===false){afterError('Falha ao rearmar',(data&&data.error)||'O backend recusou o rearme.'); return;} return Promise.all([loadStatus(true),currentTab==='operations'?loadOperations(true):Promise.resolve()]).then(()=>{if(currentTab==='nodes') renderSelectedNodeDetail(false); afterSuccess('Saída rearmada','O bloqueio manual de fail-safe foi removido.');});}).catch(()=>afterError('Falha ao rearmar','Não foi possível rearmar a saída agora.'))); return false;}
-function testFailsafeOutput(btn,outputId){if(!authCanUse('profile_write')){afterError('Acesso negado','Seu papel atual nao pode testar fail-safe.'); return false;} if(!askConfirm('Aplicar teste de fail-safe nesta saída agora?')) return false; markNodeDetailInteraction(30000); withBusy(btn,'Testando',()=>apiForm('/api/failsafe/output/test',{output_id:outputId}).then(({ok,data})=>{if(!ok||data&&data.ok===false){afterError('Falha no teste',(data&&data.error)||'O backend recusou o teste.'); return;} return Promise.all([loadStatus(true),currentTab==='operations'?loadOperations(true):Promise.resolve()]).then(()=>{if(currentTab==='nodes') renderSelectedNodeDetail(false); afterInfo('Teste aplicado','A saída recebeu a política de fail-safe de teste.');});}).catch(()=>afterError('Falha no teste','Não foi possível aplicar o teste agora.'))); return false;}
+function failsafeStateFromApi(item){return {id:n(item&&((item.output_id!=null?item.output_id:item.id))),value:n(item&&item.value),failsafe_enabled:(item&&item.enabled!=null)?n(item.enabled):1,failsafe_boot_action:String(item&&item.boot_action||'force_off'),failsafe_startup_mode:String(item&&item.startup_mode||item&&item.boot_action||'force_off'),failsafe_comm_loss_action:String(item&&item.comm_loss_action||'force_off'),failsafe_comm_loss_mode:String(item&&item.comm_loss_mode||item&&item.comm_loss_action||'force_off'),failsafe_runtime_fault_action:String(item&&item.runtime_fault_action||'force_off'),failsafe_safe_value:n(item&&item.safe_value),failsafe_recovery_mode:String(item&&item.recovery_mode||((item&&item.manual_rearm)?'manual':'auto')),failsafe_manual_rearm:n(item&&item.manual_rearm),failsafe_active:n(item&&item.failsafe_active||item&&item.active),failsafe_last_reason:String(item&&item.last_reason||item&&item.reason||'none'),failsafe_reason:String(item&&item.reason||item&&item.last_reason||'none'),failsafe_last_applied_value:n(item&&item.last_applied_value||item&&item.applied_value)};}
+function failsafeOutputTitle(item,idx){const id=n(item&&((item.output_id!=null?item.output_id:item.id))); const name=String(item&&((item.alias||item.name||item.role))||'').trim(); return name||`OUT${idx+1} · ID ${id}`;}
+function renderFailsafeStandaloneCard(item,idx){const id=n(item&&((item.output_id!=null?item.output_id:item.id))); const key=`standalone_${id}`; const st=failsafeStateFromApi(item); const canWrite=authCanUse('profile_write'); const disabled=canWrite?'':'disabled'; const boot=failsafeActionValue(st,'failsafe_boot_action','failsafe_startup_mode','force_off'); const comm=failsafeActionValue(st,'failsafe_comm_loss_action','failsafe_comm_loss_mode','force_off'); const runtime=failsafeActionValue(st,'failsafe_runtime_fault_action','failsafe_runtime_fault_action','force_off'); const safe=n(st.failsafe_safe_value)?1:0; const recovery=failsafeRecoveryValue(st); const enabled=(st&&st.failsafe_enabled!=null)?n(st.failsafe_enabled):1; const active=!!n(st&&st.failsafe_active); const address=String(item&&item.address||'').trim(); const backend=String(item&&((item.backend_name||item.backend))||'').trim(); const current=n(item&&item.value)?'ON':'OFF'; const meta=[`ID ${id}`,backend,address,`estado ${current}`].filter(Boolean).join(' · '); const actions=canWrite?`<div class="actions"><button type="button" class="soft-btn" onclick="return saveFailsafePolicy(this,${id},'${key}')">Salvar política</button>${active?`<button type="button" class="primary-btn" onclick="return rearmFailsafeOutput(this,${id})">Rearmar</button>`:''}${advancedMode?`<button type="button" class="danger-btn" onclick="return testFailsafeOutput(this,${id})">Testar</button>`:''}</div>`:'<div class="muted-line">Seu papel atual pode visualizar, mas não alterar fail-safe.</div>'; return `<div class="item"><div style="min-width:220px;flex:1"><strong>${esc(failsafeOutputTitle(item,idx))}</strong><span>${esc(meta)}</span><span>${esc(channelFailsafeText(st)||'Política segura padrão aguardando leitura.')}</span></div><div class="form" style="flex:2;min-width:280px;padding:12px"><div class="form-grid"><div class="field"><label>Ativo</label><select id="fs_enabled_${key}" ${disabled}><option value="1" ${enabled?'selected':''}>Sim</option><option value="0" ${!enabled?'selected':''}>Não</option></select></div><div class="field"><label>Boot/reboot</label><select id="fs_boot_${key}" ${disabled}>${failsafeActionOptionsHtml(boot,'boot')}</select></div><div class="field"><label>Perda de comunicação</label><select id="fs_comm_${key}" ${disabled}>${failsafeActionOptionsHtml(comm,'comm')}</select></div><div class="field"><label>Falha runtime/watchdog</label><select id="fs_runtime_${key}" ${disabled}>${failsafeActionOptionsHtml(runtime,'runtime')}</select></div><div class="field"><label>Valor seguro</label><select id="fs_safe_${key}" ${disabled}><option value="0" ${safe?'':'selected'}>OFF</option><option value="1" ${safe?'selected':''}>ON</option></select></div><div class="field"><label>Recuperação</label><select id="fs_recovery_${key}" ${disabled}>${failsafeRecoveryOptionsHtml(recovery)}</select></div></div><div class="note-strip" style="margin-top:10px">${esc(channelFailsafeText(st)||'Política segura padrão: FORCE_OFF em boot, perda de comunicação e falha.')}</div>${actions}</div></div>`;}
+function renderFailsafeTab(){if(!$('failsafe_outputs')) return; const outputs=Array.isArray(failsafeOutputs.outputs)?failsafeOutputs.outputs:[]; const enabled=outputs.filter(o=>n(o&&o.enabled)).length; const active=outputs.filter(o=>n(o&&((o.failsafe_active!=null?o.failsafe_active:o.active)))).length; const manual=outputs.filter(o=>String(o&&o.recovery_mode||'').toLowerCase()==='manual'||n(o&&o.manual_rearm)).length; text('failsafe_total',outputs.length); text('failsafe_total_sub',outputs.length?'Saídas vindas do binding ativo.':'Nenhuma saída ativa no perfil.'); text('failsafe_enabled_total',enabled); text('failsafe_enabled_sub',enabled?'Com política habilitada.':'Sem proteção habilitada.'); text('failsafe_active_total',active); text('failsafe_active_sub',active?'Há saída em condição segura.':'Nenhuma saída bloqueada.'); text('failsafe_manual_total',manual); text('failsafe_manual_sub',manual?'Rearme manual configurado.':'Recuperação automática.'); if(failsafeOutputs.error){html('failsafe_outputs',`<div class="empty">${esc(failsafeOutputs.error)}</div>`); return;} if(!failsafeOutputs.loaded){html('failsafe_outputs','<div class="empty">Abra esta aba ou clique em Atualizar fail-safe para carregar a política das saídas.</div>'); return;} if(!outputs.length){html('failsafe_outputs','<div class="empty">Nenhuma saída ativa encontrada. Use Nós > modo técnico > Binding local do nó > Adicionar saída.</div>'); return;} html('failsafe_outputs',outputs.map((item,idx)=>renderFailsafeStandaloneCard(item,idx)).join(''));}
+function loadFailsafeOutputs(force){if(!authAllowsDashboard()) return Promise.resolve(failsafeOutputs); if(!force&&failsafeOutputs.loaded&&currentTab!=='failsafe') return Promise.resolve(failsafeOutputs); return apiJson('/api/failsafe/outputs').then(data=>{failsafeOutputs={loaded:true,error:'',outputs:Array.isArray(data&&data.outputs)?data.outputs:[]}; renderFailsafeTab(); return failsafeOutputs;}).catch(()=>{failsafeOutputs={loaded:false,error:'Falha ao carregar /api/failsafe/outputs. Verifique autenticação e endpoint.',outputs:[]}; renderFailsafeTab(); return Promise.reject();});}
+function saveFailsafePolicy(btn,outputId,key){if(!authCanUse('profile_write')){afterError('Acesso negado','Seu papel atual nao pode alterar fail-safe.'); return false;} const fields={output_id:outputId,enabled:$(`fs_enabled_${key}`)?$(`fs_enabled_${key}`).value:'1',boot_action:$(`fs_boot_${key}`)?$(`fs_boot_${key}`).value:'force_off',comm_loss_action:$(`fs_comm_${key}`)?$(`fs_comm_${key}`).value:'force_off',runtime_fault_action:$(`fs_runtime_${key}`)?$(`fs_runtime_${key}`).value:'force_off',safe_value:$(`fs_safe_${key}`)?$(`fs_safe_${key}`).value:'0',recovery_mode:$(`fs_recovery_${key}`)?$(`fs_recovery_${key}`).value:'auto'}; markNodeDetailInteraction(30000); setInlineStatus('tone-info','Salvando fail-safe','A política segura da saída está sendo persistida.',true); withBusy(btn,'Salvando',()=>apiForm('/api/failsafe/output/save',fields).then(({ok,data})=>{if(!ok||data&&data.ok===false){afterError('Falha ao salvar fail-safe',(data&&data.error)||'O backend recusou a política.'); return;} return Promise.all([loadStatus(true),currentTab==='operations'?loadOperations(true):Promise.resolve(),currentTab==='failsafe'?loadFailsafeOutputs(true):Promise.resolve()]).then(()=>{if(currentTab==='nodes') renderSelectedNodeDetail(false); afterSuccess('Fail-safe salvo','A política da saída foi atualizada.');});}).catch(()=>afterError('Falha ao salvar fail-safe','Não foi possível salvar a política agora.'))); return false;}
+function rearmFailsafeOutput(btn,outputId){if(!authCanUse('profile_write')){afterError('Acesso negado','Seu papel atual nao pode rearmar fail-safe.'); return false;} markNodeDetailInteraction(30000); withBusy(btn,'Rearmando',()=>apiForm('/api/failsafe/output/reset',{output_id:outputId}).then(({ok,data})=>{if(!ok||data&&data.ok===false){afterError('Falha ao rearmar',(data&&data.error)||'O backend recusou o rearme.'); return;} return Promise.all([loadStatus(true),currentTab==='operations'?loadOperations(true):Promise.resolve(),currentTab==='failsafe'?loadFailsafeOutputs(true):Promise.resolve()]).then(()=>{if(currentTab==='nodes') renderSelectedNodeDetail(false); afterSuccess('Saída rearmada','O bloqueio manual de fail-safe foi removido.');});}).catch(()=>afterError('Falha ao rearmar','Não foi possível rearmar a saída agora.'))); return false;}
+function testFailsafeOutput(btn,outputId){if(!authCanUse('profile_write')){afterError('Acesso negado','Seu papel atual nao pode testar fail-safe.'); return false;} if(!askConfirm('Aplicar teste de fail-safe nesta saída agora?')) return false; markNodeDetailInteraction(30000); withBusy(btn,'Testando',()=>apiForm('/api/failsafe/output/test',{output_id:outputId}).then(({ok,data})=>{if(!ok||data&&data.ok===false){afterError('Falha no teste',(data&&data.error)||'O backend recusou o teste.'); return;} return Promise.all([loadStatus(true),currentTab==='operations'?loadOperations(true):Promise.resolve(),currentTab==='failsafe'?loadFailsafeOutputs(true):Promise.resolve()]).then(()=>{if(currentTab==='nodes') renderSelectedNodeDetail(false); afterInfo('Teste aplicado','A saída recebeu a política de fail-safe de teste.');});}).catch(()=>afterError('Falha no teste','Não foi possível aplicar o teste agora.'))); return false;}
 function collectChannelsFromSnapshot(node,prof,status){
  const inStates=Array.isArray(status&&status.inputs)?status.inputs:[];
  const outStates=Array.isArray(status&&status.outputs)?status.outputs:[];
@@ -1218,7 +1237,7 @@
 function renderPinConfig(){if(!$('cfg_input_id')||!$('cfg_output_id')) return; renderBindingBackendControls(); if(!$('cfg_input_id').value && (profile.inputs||[])[0]) $('cfg_input_id').value=String(profile.inputs[0].id); if(!$('cfg_output_id').value && (profile.outputs||[])[0]) $('cfg_output_id').value=String(profile.outputs[0].id); const currentInputId=$('cfg_input_id').value||''; const currentOutputId=$('cfg_output_id').value||''; const currentInputSel=$('cfg_input_gpio').value; const currentOutputSel=$('cfg_output_gpio').value; const currentAddInputSel=$('cfg_add_input_gpio')?$('cfg_add_input_gpio').value:''; const currentAddOutputSel=$('cfg_add_output_gpio')?$('cfg_add_output_gpio').value:''; const inputSelected=(inputDraft.dirty&&inputDraft.id===String(currentInputId)&&inputDraft.gpio)?inputDraft.gpio:currentInputSel; const outputSelected=(outputDraft.dirty&&outputDraft.id===String(currentOutputId)&&outputDraft.gpio)?outputDraft.gpio:currentOutputSel; $('cfg_input_gpio').innerHTML=buildGpioOptionsHtml('input',inputSelected,currentInputId); $('cfg_output_gpio').innerHTML=buildGpioOptionsHtml('output',outputSelected,currentOutputId); if($('cfg_add_input_gpio')) $('cfg_add_input_gpio').innerHTML=buildGpioOptionsHtml('input',currentAddInputSel,''); if($('cfg_add_output_gpio')) $('cfg_add_output_gpio').innerHTML=buildGpioOptionsHtml('output',currentAddOutputSel,''); if(inputDraft.dirty && inputDraft.id===String(currentInputId) && inputSelected) inputDraft.gpio=String(inputSelected); if(outputDraft.dirty && outputDraft.id===String(currentOutputId) && outputSelected) outputDraft.gpio=String(outputSelected); if($('cfg_add_input_gpio')&&currentAddInputSel) $('cfg_add_input_gpio').value=String(currentAddInputSel); if($('cfg_add_output_gpio')&&currentAddOutputSel) $('cfg_add_output_gpio').value=String(currentAddOutputSel); const addInputBackend=$('cfg_add_input_backend')?$('cfg_add_input_backend').value:'gpio'; const addOutputBackend=$('cfg_add_output_backend')?$('cfg_add_output_backend').value:'gpio'; toggleBackendAddressFields('cfg_add_input',addInputBackend); toggleBackendAddressFields('cfg_add_output',addOutputBackend); syncInputConfigFields(); syncOutputConfigFields(); html('pin_mode_guide', pinModeGuideItems().concat([{title:'MCP23x17',desc:'Use instância + canal A0..B7 para bind digital expandido. O preview automático continua valendo para GPIO local.'}]).map(item=>`<div class="item"><div><strong>${esc(item.title)}</strong><span>${esc(item.desc)}</span></div></div>`).join('')); renderSlotManagement();}
 function renderSlotManagement(){const aIn=availableInputSlots(); const aOut=availableOutputSlots(); const reserved=reservedPinsSummary(); if($('cfg_add_input_id')) $('cfg_add_input_id').innerHTML=aIn.length?aIn.map(it=>`<option value="${it.id}">${esc(it.name||`Entrada ${it.id}`)}</option>`).join(''):'<option value="">Sem slots livres</option>'; if($('cfg_add_output_id')) $('cfg_add_output_id').innerHTML=aOut.length?aOut.map(it=>`<option value="${it.id}">${esc(it.name||`Saída ${it.id}`)}</option>`).join(''):'<option value="">Sem slots livres</option>'; const addInputBackend=$('cfg_add_input_backend')?$('cfg_add_input_backend').value:'gpio'; const addOutputBackend=$('cfg_add_output_backend')?$('cfg_add_output_backend').value:'gpio'; toggleBackendAddressFields('cfg_add_input',addInputBackend); toggleBackendAddressFields('cfg_add_output',addOutputBackend); const addInputAddr=bindingAddressLabel(addInputBackend,$('cfg_add_input_gpio')&&$('cfg_add_input_gpio').value,$('cfg_add_input_backend_instance')&&$('cfg_add_input_backend_instance').value,$('cfg_add_input_endpoint_index')&&$('cfg_add_input_endpoint_index').value,'input'); const addOutputAddr=bindingAddressLabel(addOutputBackend,$('cfg_add_output_gpio')&&$('cfg_add_output_gpio').value,$('cfg_add_output_backend_instance')&&$('cfg_add_output_backend_instance').value,$('cfg_add_output_endpoint_index')&&$('cfg_add_output_endpoint_index').value,'output'); if($('cfg_add_input_hint')) text('cfg_add_input_hint', aIn.length?`Há ${aIn.length} slot(s) de entrada disponível(is). Backend: ${addInputBackend}. Endereço: ${addInputAddr}. GPIO reservados agora: ${reserved}.`:`Nenhum slot de entrada livre.`); if($('cfg_add_output_hint')) text('cfg_add_output_hint', aOut.length?`Há ${aOut.length} slot(s) de saída disponível(is). Backend: ${addOutputBackend}. Endereço: ${addOutputAddr}. GPIO reservados agora: ${reserved}.`:`Nenhum slot de saída livre.`); if($('slot_summary')) text('slot_summary', `Entradas ativas: ${(profile.inputs||[]).length}/${profile.input_slot_capacity||0} · Saídas ativas: ${(profile.outputs||[]).length}/${profile.output_slot_capacity||0} · Slots livres: ${aIn.length} entrada(s), ${aOut.length} saída(s) · GPIO reservados: ${reserved}${networkPreviewLoading?' · recalculando...':''} · Backends configuráveis agora: ${selectableBindingBackends().map(b=>b.label).join(', ')}.`);}
 function addInputSlot(btn){const id=$('cfg_add_input_id')&&$('cfg_add_input_id').value; const backend=$('cfg_add_input_backend')&&$('cfg_add_input_backend').value||'gpio'; const gpio=$('cfg_add_input_gpio')&&$('cfg_add_input_gpio').value; const backendInstance=$('cfg_add_input_backend_instance')&&$('cfg_add_input_backend_instance').value||'0'; const endpointIndex=$('cfg_add_input_endpoint_index')&&$('cfg_add_input_endpoint_index').value||'0'; const name=$('cfg_add_input_name')?$('cfg_add_input_name').value:''; const role=$('cfg_add_input_role')?$('cfg_add_input_role').value:''; if(!id||((backend==='gpio'&&!gpio)||(backend==='mcp23x17'&&endpointIndex===''))){alert('Selecione um slot e um endereço válido para adicionar a entrada.'); return;} setInlineStatus('tone-info','Adicionando entrada','O novo canal está sendo registrado no nó.',true); withBusy(btn,'Adicionando',()=>fetch(`/api/input/add?id=${encodeURIComponent(id)}&${bindingQueryParams(backend,gpio,backendInstance,endpointIndex)}&name=${encodeURIComponent(name||'')}&role=${encodeURIComponent(role||'')}`).then(r=>r.text().then(t=>({ok:r.ok,text:t}))).then(({ok,text:t})=>{if(!ok){afterError('Falha ao adicionar entrada',bindingProfileErrorText('entrada',t));return;} if($('cfg_add_input_name')) $('cfg_add_input_name').value=''; if($('cfg_add_input_role')) $('cfg_add_input_role').value=''; return Promise.all([loadProfile(),loadStatus(true)]).then(()=>afterSuccess('Entrada adicionada','O novo canal já aparece na configuração local.'));}).catch(()=>afterError('Falha ao adicionar entrada','Não foi possível adicionar a entrada agora.')));}
-function addOutputSlot(btn){const id=$('cfg_add_output_id')&&$('cfg_add_output_id').value; const backend=$('cfg_add_output_backend')&&$('cfg_add_output_backend').value||'gpio'; const gpio=$('cfg_add_output_gpio')&&$('cfg_add_output_gpio').value; const backendInstance=$('cfg_add_output_backend_instance')&&$('cfg_add_output_backend_instance').value||'0'; const endpointIndex=$('cfg_add_output_endpoint_index')&&$('cfg_add_output_endpoint_index').value||'0'; const name=$('cfg_add_output_name')?$('cfg_add_output_name').value:''; const role=$('cfg_add_output_role')?$('cfg_add_output_role').value:''; if(!id||((backend==='gpio'&&!gpio)||(backend==='mcp23x17'&&endpointIndex===''))){alert('Selecione um slot e um endereço válido para adicionar a saída.'); return;} setInlineStatus('tone-info','Adicionando saída','O novo canal está sendo registrado no nó.',true); withBusy(btn,'Adicionando',()=>fetch(`/api/output/add?id=${encodeURIComponent(id)}&${bindingQueryParams(backend,gpio,backendInstance,endpointIndex)}&name=${encodeURIComponent(name||'')}&role=${encodeURIComponent(role||'')}`).then(r=>r.text().then(t=>({ok:r.ok,text:t}))).then(({ok,text:t})=>{if(!ok){afterError('Falha ao adicionar saída',bindingProfileErrorText('saída',t));return;} if($('cfg_add_output_name')) $('cfg_add_output_name').value=''; if($('cfg_add_output_role')) $('cfg_add_output_role').value=''; return Promise.all([loadProfile(),loadStatus(true)]).then(()=>afterSuccess('Saída adicionada','O novo canal já aparece na configuração local.'));}).catch(()=>afterError('Falha ao adicionar saída','Não foi possível adicionar a saída agora.')));}
+function addOutputSlot(btn){const id=$('cfg_add_output_id')&&$('cfg_add_output_id').value; const backend=$('cfg_add_output_backend')&&$('cfg_add_output_backend').value||'gpio'; const gpio=$('cfg_add_output_gpio')&&$('cfg_add_output_gpio').value; const backendInstance=$('cfg_add_output_backend_instance')&&$('cfg_add_output_backend_instance').value||'0'; const endpointIndex=$('cfg_add_output_endpoint_index')&&$('cfg_add_output_endpoint_index').value||'0'; const name=$('cfg_add_output_name')?$('cfg_add_output_name').value:''; const role=$('cfg_add_output_role')?$('cfg_add_output_role').value:''; if(!id||((backend==='gpio'&&!gpio)||(backend==='mcp23x17'&&endpointIndex===''))){alert('Selecione um slot e um endereço válido para adicionar a saída.'); return;} setInlineStatus('tone-info','Adicionando saída','O novo canal está sendo registrado no nó.',true); withBusy(btn,'Adicionando',()=>fetch(`/api/output/add?id=${encodeURIComponent(id)}&${bindingQueryParams(backend,gpio,backendInstance,endpointIndex)}&name=${encodeURIComponent(name||'')}&role=${encodeURIComponent(role||'')}`).then(r=>r.text().then(t=>({ok:r.ok,text:t}))).then(({ok,text:t})=>{if(!ok){afterError('Falha ao adicionar saída',bindingProfileErrorText('saída',t));return;} if($('cfg_add_output_name')) $('cfg_add_output_name').value=''; if($('cfg_add_output_role')) $('cfg_add_output_role').value=''; return Promise.all([loadProfile(),loadStatus(true),loadFailsafeOutputs(true).catch(()=>{})]).then(()=>afterSuccess('Saída adicionada','O novo canal já aparece na configuração local e no fail-safe.'));}).catch(()=>afterError('Falha ao adicionar saída','Não foi possível adicionar a saída agora.')));}
 function removeInputSlot(btn){const id=$('cfg_input_id')&&$('cfg_input_id').value; if(!id){alert('Selecione a entrada antes de remover.'); return;} if(isDefaultInputId(id)){alert('As entradas padrão não podem ser removidas.'); return;} if(!askConfirm('Remover esta entrada extra?')) return; setInlineStatus('tone-info','Removendo entrada','O canal extra está sendo removido do nó.',true); withBusy(btn,'Removendo',()=>fetch(`/api/input/remove?id=${encodeURIComponent(id)}`).then(r=>r.text().then(t=>({ok:r.ok,text:t}))).then(({ok,text:t})=>{if(!ok){afterError('Falha ao remover entrada',bindingResetErrorText('entrada',t));return;} inputDraft.dirty=false; return Promise.all([loadProfile(),loadStatus(true)]).then(()=>afterSuccess('Entrada removida','O canal extra foi removido com sucesso.'));}).catch(()=>afterError('Falha ao remover entrada','Não foi possível remover a entrada agora.')));}
 function removeOutputSlot(btn){const id=$('cfg_output_id')&&$('cfg_output_id').value; if(!id){alert('Selecione a saída antes de remover.'); return;} if(isDefaultOutputId(id)){alert('As saídas padrão não podem ser removidas.'); return;} if(!askConfirm('Remover esta saída extra?')) return; setInlineStatus('tone-info','Removendo saída','O canal extra está sendo removido do nó.',true); withBusy(btn,'Removendo',()=>fetch(`/api/output/remove?id=${encodeURIComponent(id)}`).then(r=>r.text().then(t=>({ok:r.ok,text:t}))).then(({ok,text:t})=>{if(!ok){afterError('Falha ao remover saída',bindingResetErrorText('saída',t));return;} outputDraft.dirty=false; return Promise.all([loadProfile(),loadStatus(true)]).then(()=>afterSuccess('Saída removida','O canal extra foi removido com sucesso.'));}).catch(()=>afterError('Falha ao remover saída','Não foi possível remover a saída agora.')));}
 function saveInputProfile(btn){const id=$('cfg_input_id').value; if(!id){alert('Selecione a entrada antes de salvar.'); return;} const name=$('cfg_input_name').value||''; const role=$('cfg_input_role').value||''; const backend=$('cfg_input_backend').value||'gpio'; const gpio=$('cfg_input_gpio').value||''; const backendInstance=$('cfg_input_backend_instance').value||'0'; const endpointIndex=$('cfg_input_endpoint_index').value||'0'; setInlineStatus('tone-info','Salvando entrada','As mudanças do canal estão sendo aplicadas.',true); withBusy(btn,'Salvando',()=>fetch(`/api/input/config?id=${encodeURIComponent(id)}&name=${encodeURIComponent(name)}&role=${encodeURIComponent(role)}&${bindingQueryParams(backend,gpio,backendInstance,endpointIndex)}`).then(r=>r.text().then(t=>({ok:r.ok,text:t}))).then(({ok,text:t})=>{if(!ok){afterError('Falha ao salvar entrada',bindingProfileErrorText('entrada',t));return;} inputDraft.dirty=false; return Promise.all([loadProfile(),loadStatus(true)]).then(()=>afterSuccess('Entrada salva','A configuração do canal foi atualizada.'));}).catch(()=>afterError('Falha ao salvar entrada','Não foi possível salvar a entrada agora.')));}
@@ -1254,6 +1273,7 @@
  renderNodes();
  renderLearn();
  renderPinConfig();
+ renderFailsafeTab();
  updateLiveChip();
 }
 function loadStatus(silent){return apiJson('/api/status').then(d=>{updateStatus(d); return d;}).catch(()=>{if(!silent&&window.console) console.warn('status refresh failed');});}
@@ -1267,7 +1287,7 @@
 ['cfg_input_name','cfg_input_role','cfg_input_gpio','cfg_input_backend','cfg_input_backend_instance','cfg_input_endpoint_index'].forEach(id=>document.addEventListener('DOMContentLoaded',()=>{const el=$(id); if(!el) return; el.addEventListener((id==='cfg_input_name'||id==='cfg_input_role')?'input':'change',()=>{inputDraft={dirty:true,id:$('cfg_input_id')?String($('cfg_input_id').value||''):'',backend:$('cfg_input_backend')?String($('cfg_input_backend').value||'gpio'):'gpio',gpio:$('cfg_input_gpio')?String($('cfg_input_gpio').value||''):'',backend_instance:$('cfg_input_backend_instance')?String($('cfg_input_backend_instance').value||'0'):'0',endpoint_index:$('cfg_input_endpoint_index')?String($('cfg_input_endpoint_index').value||'0'):'0',name:$('cfg_input_name')?$('cfg_input_name').value:'',role:$('cfg_input_role')?$('cfg_input_role').value:''}; syncInputConfigFields(); renderSlotManagement();});}));
 ['cfg_output_name','cfg_output_role','cfg_output_gpio','cfg_output_backend','cfg_output_backend_instance','cfg_output_endpoint_index'].forEach(id=>document.addEventListener('DOMContentLoaded',()=>{const el=$(id); if(!el) return; el.addEventListener((id==='cfg_output_name'||id==='cfg_output_role')?'input':'change',()=>{outputDraft={dirty:true,id:$('cfg_output_id')?String($('cfg_output_id').value||''):'',backend:$('cfg_output_backend')?String($('cfg_output_backend').value||'gpio'):'gpio',gpio:$('cfg_output_gpio')?String($('cfg_output_gpio').value||''):'',backend_instance:$('cfg_output_backend_instance')?String($('cfg_output_backend_instance').value||'0'):'0',endpoint_index:$('cfg_output_endpoint_index')?String($('cfg_output_endpoint_index').value||'0'):'0',name:$('cfg_output_name')?$('cfg_output_name').value:'',role:$('cfg_output_role')?$('cfg_output_role').value:''}; syncOutputConfigFields(); renderSlotManagement();});}));
 ['cfg_add_input_backend','cfg_add_input_backend_instance','cfg_add_input_endpoint_index','cfg_add_input_gpio','cfg_add_output_backend','cfg_add_output_backend_instance','cfg_add_output_endpoint_index','cfg_add_output_gpio'].forEach(id=>document.addEventListener('DOMContentLoaded',()=>{const el=$(id); if(el) el.addEventListener('change',renderSlotManagement);}));
-window.addEventListener('load',()=>{authRestoreSessionToken(); currentTab='panel'; initNodeDetailInteractionTracking(); if(!refreshTimer){refreshTimer=setInterval(()=>{if(document.hidden||!authAllowsDashboard()) return; if(!wsConnected) loadStatus(true); if(currentTab==='nodes' && (Date.now()-lastNodesRefreshAt)>=NODE_BACKGROUND_REFRESH_MS) refreshNodesSoon(false,0); if(currentTab==='operations') loadOperations(false);},2500);} if(!authTimer){authTimer=setInterval(()=>{if(document.hidden) return; if(!authAllowsDashboard()) return; refreshAuthStatus(false,authEpoch).catch(()=>{});},15000);} refreshAuthStatus(false,authEpoch).catch(()=>{}); updateLiveChip();});
+window.addEventListener('load',()=>{authRestoreSessionToken(); currentTab='panel'; initNodeDetailInteractionTracking(); if(!refreshTimer){refreshTimer=setInterval(()=>{if(document.hidden||!authAllowsDashboard()) return; if(!wsConnected) loadStatus(true); if(currentTab==='nodes' && (Date.now()-lastNodesRefreshAt)>=NODE_BACKGROUND_REFRESH_MS) refreshNodesSoon(false,0); if(currentTab==='operations') loadOperations(false); if(currentTab==='failsafe') loadFailsafeOutputs(false);},2500);} if(!authTimer){authTimer=setInterval(()=>{if(document.hidden) return; if(!authAllowsDashboard()) return; refreshAuthStatus(false,authEpoch).catch(()=>{});},15000);} refreshAuthStatus(false,authEpoch).catch(()=>{}); updateLiveChip();});
 </script>
 </body>
 </html>
